# Windows binaries used to be tracked here via Git LFS. The LFS
# pointers were served by Vercel (only 134 bytes — the pointer text,
# not the actual file) and the download button gave users a broken
# non-executable. Binaries are now hosted as GitHub Release assets
# instead; the website's download button points to the release URL.
